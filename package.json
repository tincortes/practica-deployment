{
  "name": "mi-portafolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "vite": "^5.0.0",
    "laravel-vite-plugin": "^0.8.0"
  }
}
